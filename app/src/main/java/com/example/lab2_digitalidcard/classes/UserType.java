package com.example.lab2_digitalidcard.classes;

import com.example.lab2_digitalidcard.data.DBDAO.UserProfileDao;

public class UserType {
    //check if there is userprofile info in local db
/*    public boolean isUserProileExist(){
        if (new UserProfileDao().getAllRecords().size() > 0)
            return true;
        else
            return false;
    }*/
}
